#include <stdio.h> // Include information about standard library

// Define a function called main that received no argument values statements of
// main are enclosed in braces main calls library function printf to print this
// sequence of characters \n represents the newline character
int main() { printf("Hello, world\n"); }
